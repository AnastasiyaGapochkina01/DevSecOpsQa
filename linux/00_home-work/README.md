# Linux System Administration Automation Script

## 📋 Описание

Этот bash-скрипт автоматизирует выполнение задач по системному администрированию Linux. Скрипт создает пользователей, настраивает права доступа, собирает системную информацию, устанавливает сервисы и выполняет другие административные задачи.

## 🎯 Задачи, выполняемые скриптом

### Задача 1: Сбор системной информации
- Создает директорию `practice` в домашней директории
- Создает файл `system-info` с информацией о системе:
  - Текущая дата и время
  - Размер диска, смонтированного под корневую директорию
  - Общее количество оперативной памяти
  - Количество ядер CPU
  - Текущий load average
  - 3 директории в системе, которые занимают больше всего места

### Задача 2: Управление пользователями и группами
- Создает пользователя `devops` и группу `external`
- Добавляет пользователя `devops` в группу `external`

### Задача 3: Работа с файловой системой
- Создает директорию `/opt/projects` с файлами:
  - `app.py`
  - `run.sh`
  - `main.go`
- Рекурсивно назначает владельцем директории `projects` пользователя `devops`, а группой - `adm`
- Настраивает права доступа:
  - `run.sh`: исполняемый для всех
  - `app.py`: владелец - полные права, группа - чтение и запись, остальные - нет прав
  - `main.go`: владелец - чтение и запись, группа - чтение и выполнение, остальные - только чтение

### Задача 4: Резервное копирование
- Создает сжатый архив директории `projects` в `/opt/backups`

### Задача 5: Сетевые интерфейсы
- Выводит все сетевые интерфейсы в статусе UP с их именами и IPv4 адресами

### Задача 6: Управление процессами
- Запускает процессы `sleep` для анализа родительских связей
- Находит PID и PPID процессов `sleep`
- Запускает процесс `yes` для анализа потребления ресурсов
- Записывает 3 процесса с максимальным потреблением CPU в `top_cpu`
- Записывает 3 процесса с максимальным потреблением памяти в `top_mem`
- Завершает процесс `yes`

### Задача 7: Установка и настройка сервисов
- Устанавливает `nginx` и `memcached`
- Создает файл `external_services` с информацией:
  - Запущены ли сервисы `nginx` и `memcached`
  - Добавлены ли они в автозагрузку
  - Формат: `<имя_ВМ> <дата_время>`

### Задача 8: Настройка прав sudo
- Дает пользователю `devops` права на выполнение команд `systemctl stop/start/restart/status` для `nginx` и `memcached` без пароля

## 🚀 Установка и запуск

### Предварительные требования
- Операционная система: Linux (Ubuntu/Debian)
- Права суперпользователя (sudo)
- Доступ к интернету для установки пакетов

### Установка

1. Скачайте скрипт:
```bash
wget -O linux_admin_script.sh https://raw.githubusercontent.com/yourusername/repo/main/linux_admin_script.sh




